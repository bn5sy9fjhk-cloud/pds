# 霍桐 PDS V1.0 前端 UI/UX 设计方案

> 依据（唯一事实来源）：`霍桐PDS-V1.0技术设计方案(修订版).md`。
> 前端基准：uni-app (Vue3)。预览内核：provider-pluggable（M4 前 PoC 后选型，UI 无关具体内核）。
> 后方可锁定：本文件为设计输入，不生成业务代码；确认后，再进入页面原型 HTML/CSS（uni-app vue 页面骨架）阶段。
> 约定：页面中出现的「权限码」均为技术方案 §5.2 固化码字面，不新增；标「预留 / M4+」者当期末开放给用户，只能显示"暂不支持/能力即将开放"，不得伪装成已支持。

---

## 第一部分 设计理念与视觉方向

### 1.1 产品定位与气质
霍桐PDS 是**霍桐仪器的企业内部文件与工程资料数据管理平台**：企业网盘 + 部门共享文件空间 + CAD/3D/脚本等工程技术资料管理。

视觉 = **专业、克制、稳定、高级、可长期维护**。
对标的不是单一产品，而是下述设计逻辑的融合：
- 文件操作效率 —— 阿里云盘桌面端 / Finder / Explorer；
- 信息密度与企业感 —— 阿里云控制台；
- 清晰层级 —— 飞书；
- 克制排版 —— Notion；
- 紧凑现代 —— Linear。

### 1.2 视觉支柱（Design Pillars）
1. **效率优先**：一次可见、一指即达。列表模式为第一优先级；常用操作不藏进菜单；键盘与多选同样重视。
2. **信息层级**：页面不超过 3 级视觉深度；用留白/边框/字号/色阶分区，靠结构而非卡片堆砌。
3. **平面的主体**：主内容区避开独立悬浮大卡片，采用分隔线+浅色 surface 分区；**阴影只出现在悬浮层级**（弹层/菜单/浮窗/详情栏）。
4. **克制圆角与色彩**：主色仅用于主按钮/选中/关键操作；危险统一红、成功统一绿、警示统一琥珀。大面积渐变 = 禁止。
5. **动效有目的且轻微**：淡入/悬停/目标高亮/进度/菜单/弹层；禁止弹跳、漂浮、呼吸光效、无意义放大缩小。
6. **两套一致的设计语言**：用户端与管理端同 token、同组件（管理端只是更"密"，不做成另一套老系统皮肤）。

### 1.3 明确禁止的视觉
- 普通后台模板感 / AI 拼贴卡片墙；
- 过度圆角(16/20px 大号)用在普通容器；
- 大面积渐变背景、高饱和主色铺底、五颜六色点缀；
- 夸张阴影、无意义的"数据大屏"+花哨图表堆叠；
- 营销官网式 hero、轮播、插画 banner；
- 低端"网盘 UI"（超大下载箭头、拟物文件夹、彩色 icon 混排 emoji）；
- 传统臃肿企业 OA（左侧多级手风琴 + 密密麻麻导航 + iframe shell 感）。

### 1.4 布局语言
- 采用「顶栏 + 窄侧栏 + 内容区」标准工作台结构，窄侧栏默认 216px（可收起至 64px），信息密度由组件密度承担而非挤压。
- 内容区以**可扫读列表为骨、分栏细节为翅**：文件列表、右侧可选详情栏(320px，可折叠)。
- 页面之间切换支持轻微淡入(140~180ms, ease-out)；列表数据变化不做整表抖动，仅对变化行做 bg 高亮闪烁(≤600ms)后还原。

---

## 第二部分 设计系统（Design Tokens & 规范）

### 2.1 Color Tokens
统一走 CSS 变量 / uni.scss `vars.scss`，语义化命名，禁止在页面写裸色值。

| Token | 色值建议 | 说明 |
|---|---|---|
| `--color-primary` | `#2454E1`(科技蓝，偏深不刺眼) | 品牌色主色；主按钮/选中/活动 |
| `--color-primary-hover` | `#1D45BC` | |
| `--color-primary-active` | `#173994` | |
| `--color-primary-tint` | `#EDF2FF` | 极轻蓝底：选中行、活动项、框架 |
| `--color-text-primary` | `#1A1F2B` | 主文字/文件名词条 |
| `--color-text-secondary` | `#5A6272` | 元信息、说明 |
| `--color-text-tertiary` | `#8B93A7` | 占位、禁用性弱文字 |
| `--color-text-inverse` | `#FFFFFF` | |
| `--color-border` | `#D8DCE4` | 列表分隔/描边 |
| `--color-border-light` | `#EBEDF1` | 极细分隔 |
| `--color-background` | `#F5F6F8` | 应用底色 |
| `--color-surface` | `#FFFFFF` | 内容主底 |
| `--color-surface-hover` | `#EDF0F4` | 行/卡片 hover |
| `--color-surface-selected` | `#E6ECFC` | 选中行（偏主色 tint） |
| `--color-success` | `#1F9D61` | 上传完成/成功 |
| `--color-success-tint` | `#E6F6EE` | |
| `--color-warning` | `#D98E00` | 处理中/待续/警告 |
| `--color-warning-tint` | `#FDF3E2` | |
| `--color-danger` | `#D63B3B` | 删除/销毁及危险确认 |
| `--color-danger-tint` | `#FBECEC` | |
| `--color-info` | `#5B6CK9`→与 primary 协调，仅提示用 | |

语义要点：页面背景 **不在大面积铺品牌色**；`--color-background` 灰底 + surface 白色主体 + tint 坐选中/高亮。品牌蓝是"施动/选中"，不做墙面漆。

### 2.2 Typography（字号体系，基于 16/rem 端 ui 设计稿）
| 角色 | 字号/字重 | 用法 |
|---|---|---|
| 页面标题 | 20px / SemiBold | 目录名/面板大标题，常配面包屑 |
| 模块标题 | 16px / SemiBold | 区块头、管理端页面标题、右侧详情栏头 |
| 正文 | 14px / Regular | 默认文本 |
| 表格文字 | 13px / Regular | 文件列表正文元信息紧凑成 13 |
| 文件名称 | 14px / Medium | 文件名可点(open) |
| 文件元信息 | 12px / Regular，`--text-tertiary` | 大小/时间/修改人 |
| 辅助说明 | 12px / Regular，`--text-tertiary` | 空态、tips、权限提示 |
| 按钮文字(primary/ghost) | 14px / Medium | 工具栏、（小 13px 用于紧凑列表操作项） |
| 标签文字 | 12px / Medium，(caps 仅用于"角标类空间类型") | 文件类型 tag / badge / 明细 key |

- 行高 1.5~1.6；标题 1.3。
- 文件列表表头与正文用相同 13px；列垂直 32~36px 高为基准。
- **不允许**一屏上随意出现 11/13/15/18/20 混乱混排。

### 2.3 Spacing（间距体系 4 进制）
间距基准原子：`4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 48`。
- 页面外边距：`24px`（1280 起由内容自适应）。
- 内容区块间纵向间距：`16~24`；同一面板内：`8/12`。
- 列表行高 36；文件行名称与其下元信息行距 4；间距语义详见第四部分文件列表。

### 2.4 Radius（小圆角体系）
| 层级 | 值 | 应用 |
|---|---|---|
| `--radius-xs` | `2px` | 细 chip、状态点 |
| `--radius-sm` | `4px` | 输入框/按钮/下拉/检查框/tag |
| `--radius-md` | `6px` | 卡片/弹窗/上传缩略/右侧面板头部 |
| `--radius-lg` | `10px` | 大模态/菜单尽量不超 10 |

普通容器默认 `sm=4` 为主。**不采用 16/20px 大圆角包裹。**

### 2.5 Elevation（阴影仅给悬浮层）
| 级别 | token | 用途 |
|---|---|---|
| `--shadow-none` | 无 | 常规页面平面 |
| `--shadow-base`(raised) | `0 1px 2px rgba(20,30,60,.05)` | 轻悬停的卡片(极少) |
| `--shadow-popover` | `0 4px 12px rgba(20,30,60,.12)` | 右键菜单/下拉/Popover/拖拽层/上传浮窗 |
| `--shadow-modal` | `0 8px 28px rgba(16,26,54,.18)` | Modal/抽屉 |
右侧详情栏(贴内容区)不上浮、与列表共用同一 surface 一层分隔即可（如 header breadcrumb 与工具栏同平面）。

### 2.6 Icon（统一线性一套 + 文件类型图标）
- 一个线性图标库(或自制同名源，统一 **24px 网格 / stroke=1.5** )，禁 emoji 代替。
- 尺寸标准：行内 16、列表/网格中文件类型图标 20、工具条图标(带文字)16、状态点 10、侧栏导航 18–20；详情/上传缩略 32。
- 图标命名 vs 权限/交互语义（如 lock=仅查看、pin、star、trash、download、eye）放 icon 清单集中注册，供 strictimport。

**文件类型图标设计策略**（统一"底 + 线性微笔触 + 角标"语言）：
**文件类型图标设计策略**（功能性，非拟物）：统一“描边矩形底 + 专业符号 + 中性色为主”，保证同族辨识而不花哨：
- 文件夹：蓝色 tint 底 + 干净描边轮廓；
- 图纸(2D)族：在线稿坐标风格下作 dwg/dxf 两态变体(偏移)；
- 3D 族：以立方体棱线为共同语言，step/stl/obj/fbx/glb 以小字号差异角标区分，不为一族开多个造型以免失家族性；
- 其余（image 圆框图、pdf 折角、doc/xls/ppt 图标、压缩包拉链、代码 `{}`/脚本、文本、未知通用文件）中性为主；特定类型仅承载辨识力，不做拟物或渐变填充。
- 清单（命名常量映射扩展名源见 `constants/fileTypes.ts`，UI 不硬编码扩展列表）：
  `folder · image · pdf · word · excel · ppt · cad-dwg · cad-dxf · model-3d · model-step · model-stl · model-obj · model-fbx · model-glb · code · archive · text · video · unknown`。

### 2.7 状态配色(通用徽/点数组) 
成功/绿色、进行/琥珀、失败与危险红、禁用(45% 灰)，与文件列表语义严格对齐 §8 交互规范。

---

## 第三部分 产品信息架构（IA）

### 3.1 用户端导航（顶栏 + 左 IA）
```
[ Header ]  Logo·霍桐PDS   [全局搜索]        [+上传] [通知] [头像▾ 用户/空间用量]
[ 左侧栏 ] 我的文档  共享空间            ↘底置 个人盘容量条(used/quota)
            最近    收藏
            回收站
[ 内容区 ]  面包屑 / 目录  工具栏(随选择态)  文件列表(列表/网格)  [可选 右侧详情栏]
```
- 顶栏放**全局搜 + 上传**(最常触达)；左栏放**视图根**：我的文档(个人盘)、共享空间(列出我可见的部门/共享区)、最近、收藏、回收站。
- 空间与「我的文档」的关系：共享空间作为可切换的空间集合层（SpaceSwitcher 视图）进入，内部沿用与个人文档一致的文件浏览组件。
- 左侧底部：个人盘容量条(used / quota)紧凑条+数字。
- 首屏(登录后) 默认进入**我的文档**（主流网盘行为，不做空彩卡“工作台”首页铺大屏），目录空态规范见 §4.5。

### 3.2 管理端导航（同设计语言，"密级"更密）
- 顶栏同用户端（左下 Logo、右侧头像/退出），但入口以**角色可见**，非管理员不显示。
- 左栏(管理员)：
  概览 / 组织成员 / 空间管理 / 空间成员权限 / 配额 / 文件管理 / 回收站 / 审计日志 / 系统配置（分组：运营 / 治理 / 运维）。
- 管理端不背包裹式多层树；用左栏顺序 + 页面内子 Tab。

### 3.3 页面路由骨架(命名后续工程映射)
用户端： `home/files/…`, `space`, `recent`, `starred`, `trash`, `search`, `share`, `settings/…`, `preview/view`（全屏预览）。
管理端： `admin/overview`, `admin/org`, `admin/drives`, `admin/drive/:id/members`, `admin/quota`, `admin/admin-file`, `admin/recycle`, `admin/audit`, `admin/config`。

### 3.4 页面级导航基本原则
- 同一份「返回文件列表」语义统一由用户端栈内返回完成，不套 iframe。
- 移动端沿用 uni 原生栈式页面导航，底部主 Tab 收敛为「文件 / 上传 / 最近 / 我的」四盏（具体见第六部分；「上传」为队列入口、「我的」聚合设置/收藏/回收站），避免手机端与桌面功能语义分裂。

---

## 第四部分 用户端 PC 核心页面逐页设计

> 所有操作按钮的显隐/可用性由 `DRIVE_READ/FILE_DOWNLOAD/FILE_PREVIEW/FILE_CREATE/FILE_WRITE/FILE_MOVE_COPY/FILE_DELETE/FILE_RESTORE/MANAGE/owner`（§5.2 固化权码）驱动。前端只控制 UI 呈现 → 最终放行仍由后端 `@RequireDrivePerm`。UI 提供“被禁/不可用仍需可见但灰显+可解释原因”，而非把入口全隐藏后让用户靠后端报错猜。

### 4.1 页面全局壳 AppLayout
- 三区布局：Header(56) + Sidebar(216/64 collapsible) + 内容(自适应)。
- Header 固定 56：左 `Logo(20px 描边 企业标) + 霍桐PDS`；中**全局搜索**（可折叠触发 `Ctrl/Cmd+K`），宽 ≤ 480；右侧 `[+ 上传](primary)`、通知铃(红点, 下拉)、头像菜单(我的设置/退出/管理入口按角色)。
- Sidebar 分组「功能」下五个一级项(我的文档/共享空间/最近/收藏/回收站)；底部「空间用量」：所在盘名 + 容量条 + `已用/配额`（已用与额度均用统一 format 展示，如 `12.4 GB / 100 GB`）。使用率超 85% 变 warning 文案「剩余偏少」，达上限 danger。该条数据由 `/api/drives` 随空间列表一并返回（是否含 capacity 见第十一部分待确认清单），否则收敛为仅显示容量框架并隐藏数字。
- 内容区 = Breadcrumb+工具栏+列表 + 可选右侧详情栏；折叠详情栏随记忆。

### 4.2 主界面布局（第一优先级页面)
```
┌──────────────────────────────────────────────────────────────┐
│ Header: 霍桐PDS  [Ctrl/Cmd+K 全局搜索]       +上传 · 通知 · 头像(name/…) │
├──────────┬──────────────────────────────────────────────────────│
│ 我的文档  │  我的文档 > 项目/工程图纸 > 3D夹具   [列表|网格][排序…]│
│ 共享空间  │  ─ 面包屑：当前空间(回根) · 父目录 · 当前目录(高亮)   │
│ 最近      │                                                      │
│ 收藏      │  ┌ 工具栏（随选择上下文）──────────────┐              │
│ 回收站    │  │ [上传][新建文件夹]      (多选用后出现) │             │
│ ──────── │  │              [下载][移动][复制][更多…]│             │
│ 空间用量  │  └──────────────────────────────┘              │
│ 盘容量条  │  │ 名称 │ 类型 │ 大小 │ 修改时间 │ 修改人            │
│          │  ├ .folder 项目资料_图纸  ·  ·  ·             ⋯       │
│          │  └ .file  3D夹具图.dwg  DWG  2.3MB  2分钟前  张工  ⋯ │
└──────────┴──────────────────────────────────────────────────────┘
        侧栏 可折叠 216/64   内容区自适应   选中/悬停行操作「⋯」
```
- **面包屑** 顶层 = 当前空间(可点回「空间根」)，其后逐目录；点击任意面包屑段可跳。不显示无关的 drive UUID。
- **工具栏策略**：
  - 无选择：上传 / 新建文件夹 /（已进入已选态会有 更多→…）—— 新建/上传按钮来自 FILE_CREATE;
  - 选择后：出现可用操作由权码决定：下载(FILE_DOWNLOAD) 移动(FILE_MOVE_COPY) 复制(FILE_MOVE_COPY) 删除(FILE_DELETE)；
  - 多选混合(文件夹+文件)的下载：MVP 若后端未提供“目录打包/多文件打包”下载，则工具栏的「下载」仅在选中**单个文件**时可用；对文件夹/多选项禁用并 tooltip 说明“暂只支持单个文件下载”，避免造成一定会失败的操作（打包下载作为待确认 P2，见待确认清单 C）。
- **列表 vs 网格**：默认列表; 顶栏提供切换图标按钮(列表/网格两态)。记忆每 drive+目录。
- **排序/筛选控件**：表头可排序字段 = 名称 / 大小 / 修改时间。文件夹始终置顶（两桶分别排序，翻转“名称”仅切换文件的顺序，不与文件夹混排）。列表采用 Keyset 滚动分页加载；排序/筛选随 `?sort=&filter=` 传回后端。

### 4.3 文件列表（列表模式 第一重点）
- 表头行固定：`勾选`+`名称(扩展图标)`+`类型`+`大小`+`修改时间`+`修改人`+(hover 出现的「更多」列空)。
- 行高 36，名称列 icon 20 >> 名称(14/Med)+ 同格副行显示状态徽标/新版标识。
- **名称分组元信息**：(主)+可选状态 tag(如「处理中」「仅查看」「共享」)；
- 列对齐：大小右对齐等宽、时间与人固定时效 fmt。
- 修改人列显示后端返回的展示名（如用户真实姓名/昵称，来自对象 `updated_by` 关联的成员信息）；不做本地额外推算，为空则该格回显 `—`。
- checkbox (8px 圆)：表头支持全选、Shift 连续、Ctrl/Cmd 加减选（沿用浏览器原生多选行为）。
- **双击语义**：双击目录=进入目录；双击文件（有 `FILE_PREVIEW`）=打开统一预览（见 §4.15）。命名不与双击绑定；单击一次并稍停可进入命名编辑，或以 F2 触发重命名（见 §8）。无预览权限时双击无响应或由行为降级为选中，不抛报错。
- 每行右侧 hover 显示「⋮」更多下拉(重命名/移动/复制/下载/预览/版本/删除移入回收站)。
- **缩略意图**：列表模式暂不放缩略图（密度优先、效率）；网格放 thumb（有后端缩略产物时）；网格缺图 fallback 用类型图标。

### 4.4 网格模式（次优先）
- 卡片规整铺满网格（顶部缩略位占位，文件名 1 行截断 + 小类型徽）；
- 悬停浮出「⋮」与右上角勾选小选区；
- 卡片尺寸固定区间（如 128~172 宽、高约 140），流式网格铺排；
- 缩略图仅在对象具备后端可产出的缩略（按预览类型 image/model/thumb 有产物）时加载（数据来源同预览信息接口的产物地址 `/t/{hash}`）；无缩略或不可预览则回落为类型图标；产物就绪才渲染，不存在则以图标占位并定时刷新。

### 4.5 关键状态与空态展示
文件列表必须能正确呈现如下状态（分别由后端返回码或本地状态派生）：
1. **空目录**：中性线性图标(`folder-open`) + 一句说明「此目录还没有内容」；若有 `FILE_CREATE` 附「上传文件 / 新建文件夹」主按钮；只读场景仅文案提示无按钮。
2. **加载中**：初次载入用骨架行（skeletonRows / shimmer，不闪烁不跳动）；分页滚动加载只在底部出细占位行。
3. **无权限访问空间/资源**：侧栏不出的空间入口不显示也不路由；但通过直链/外部入口进入预览等能力不足时，目标页显示明确的「无权预览」/「无权下载」说明卡（不给一个点了才报错、也不给必失败的“按钮装饰”）。
4. **上传中行**：进行/新建中的上传不把前置占位(INIT)对象渲染进列表视图（后端 `storage_object` INIT 态不出现在普通列表），进度一律在「上传中心」体现，用户回到目录只看到最终就绪结果——避免半途幽灵行与假命名。仅「即将出现」的提示以目录顶部细条(在此目录有 N 个上传正在写入)温和提示。
5. **文件处理中**：`preview` 排程或异步处理后状态的文件，于预览页状态或该行文件名徽标显示 `processing`（琥珀）。
6. **同名冲突**：见 §4.7 上传；重命名冲突同理并发后返回「目标已存在」走改名对话框，不静默。
7. **删除确认**：统一 danger 确认弹窗（文案因场景不同），见 §4.12。
8. **移动/复制进行中**：批量跨目录/递归场景后端以异步 job 执行；顶部一非阻塞小条(「正在移动 12 个项…」可收起)，完成/失败给 summary + 刷新受影响目录，不整页转圈。
9. **网络异常**：顶部细条错误 + 重试；不上报竞态的双重要求，见 §8。

### 4.6 上传中心（独立任务管理组件 UploadTaskCenter — 第一优先）
定位：不是简单提示框。全局唯一上传引擎 + 可停驻右下角的面板组件，Store 持久管理队列（刷新仍持持有未完成 task，后端为权威）。
- **状态必须严格对接后端 §9/17.4 状态机**：`INIT / UPLOADING / PAUSED / COMPLETING / SUCCESS / FAILED / CANCELLED / EXPIRED`；前端仅补充两个**流程性的排队派生态**`queued`(并发窗口内等位)与`checking`(md5 秒传比对)，绝不把派生态当最终完结状态，最终语义一律以任务 `status` 回执为准（避免自造后端没有的完成态）。
- 同 requestId 幂等由队列保证——queue 直接以后端返回的 `taskId` 为权威引用，天然满足重试不重复。
- 每行展示：类型 icon + 文件名，目标路径(截断到父链) + 文件大小；进度细条（primary；失败 danger；暂停暂用的“冷却灰”）、实时速率与剩余时间；右侧操作按状态显示 [暂停/继续][取消] 或 [重试]。
- 秒传命中(`/api/file/quick` 命中)同样给成功回执，面板上短暂呈现或直接通知条，防用户误以为未传。
- 面板头统计行：`↑ N 个上传中 · 已完成 M · 失败 K`。
- 收起为右下角**迷你胶囊**(icon + 计数/红点)，悬停弹出最近 3 条与进度，可展开大面板、可关闭但后台仍在跑（之后点击顶栏“↑上传”可重新打开）。
- 入口统一：工具栏“上传”、拖拽 OS 文件/文件夹、右键“上传新版本到此文件”→ 全入同一队列；H5 选整目录经 adapter 递归得到相对路径骨架(逐级 mkdir 后落文件)。

### 4.7 上传遇到的“同名冲突”处理 UI（严格对应修订 §8 覆盖 / 自动改名）
- 数据来源：上传前初始（md5 命中或目标路径存在）由后端判定并返回决策点，前端不做擅自替换决定；目标已存在且无写权时后端给 `NEED_COVER_PERM`，UI 据此展示，见下。
- 冲突以**逐文件冲突卡**呈现于上传中心（不开批量静默）：
  - 「保留两者（自动改名）」默认项 → 落成 `原名(1)` 新对象，需 `FILE_CREATE`；成功后列表以新显示名出现并 toast 说明。
  - 「覆盖当前文件 (成为新版本)」→ 上传后作为新版本(cover，不改 node) 需 **`FILE_WRITE`**；无该权则此项禁用并 tooltip「您没有编辑该文件的权限」。
  - 「跳过」
- 确认后任务真实入队并把所择语义写回 task/上传参数，最终行为仍由后端 `version--`/`auto-renumber` 决定（对应 §8 表格），前端绝不自行覆盖。

### 4.8 拖拽设计（桌面 H5 专，优先支持）
三向统一 DropZone 语义调停 + 轻微动画（禁止夸张拖拽动画）：
- A) **OS 文件/整目录拖入上传**：视口作为热区；拖入时顶部居中浅色引导条提示目标目录(当前目录/相关目录)，目录高亮；松开后把 dataTransfer 文件及文件夹递归出相对路径骨架入队 §4.6，不阻塞当前列表。
- B) **网盘内拖动移动/复制**：拖动项为“列表 icon+文件名”的半透明精灵(payload)。可放目标（侧栏目录/面包屑当前目录可放的 folder 空间区，仅限**本盘**结构）显示主色高亮；不可放位置统一 `not-allowed` 光标 + 轻微透视。松开= move（源需 `FILE_MOVE_COPY`/`DRIVE_READ`，目标需目标夹写入能力见 §5，UI 依据即放即算）；移动以异步按目录刷新，成功后 toast「已移动到 {目录}」；同盘内若移动遇重名 → toast「目标已存在，已保存为 {原名(1)}」与 §8 自动改名一致。跨空间移动不在“拖放”上提供(易错)，一律走“移动到…”对话框选目标盘/目录。
- C) **拖到左侧「回收站」删除**：本盘内直接 drop 到回收站项=删除动作；因 PDS 删除实为“移入回收站”，drop 后先出确认卡「将 {n} 个项移入回收站？恢复期内仍可还原」[移入][取消]，确认后才执行删除（需 `FILE_DELETE`；不支持的项说明）。不会出现“直接销毁”。
- 允许/禁止以绿色高亮 vs 禁光标表达，无怪异特效。移动完成统一刷新并给反馈。

### 4.9 文件右键菜单
- 对象行右键菜单项按权限计算是否可用：打开(预览/进入)、在详情栏查看、下载、移到、复制到、重命名（F2）、上传新版本（到普通文件，同盘）、删除（移入回收站）。
- 动作项只显示该项存在且“功能虽缺能力则以**禁用 + tooltip 原因**（如『需要编辑权限 FILE_WRITE』）呈现”，让“权限不足”与“本无此功能”可区分、减少盲试。不做误导的纯图标垃圾菜单。
- 空白区右键=目录页菜单：刷新、新建文件夹、粘贴(若有剪贴板移动源且目标可写)、上传。

### 4.10 右侧详情栏 RightDetailPanel（可折叠）
桌面文件列表可视需要展开右侧详情栏(宽度 320，紧贴内容区一侧，与列表共用一平台，不浮)。
- 顶部：类型 icon/缩略(64) + 文件名 + [⋮] 重命名入口(按 `FILE_WRITE`) + 扩展名 tag。
- 基本：文件大小、修改时间、修改人(`updated_by`)。
- 归属：空间名 + 当前父(可点返回定位)、所在路径链。
- 指纹：受保密策略约束——**MD5/指纹仅在用户具备 `FILE_DOWNLOAD`(有权下载) 时展示**；否则该条目省去并注「无下载权限不显示指纹」；创建人/创建时间/当前版本号按可见性给出。
- 我的权限：一行 chip(现有 drive 权码子集)+ owner / 空间管理者标记；是 owner/MANAGE 才出现“管理/成员”入口。
- 版本(文件且有历史)：当前版本(版本号/修改者/note)+ 历史列表(逐一版本随时间/人/note)；操作：恢复历史版本(将作为新版本，需写权 FILE_WRITE + 恢复语义见修订 §8/17) 或删除历史(非当前；owner 或 MANAGE 可) —— 当前版本不可删除(后端也禁止)。
- 分享入口在本版本**暂不放置**：不对（M5 才可能具备的）后端外链/内部分享能力做假占位，以避免误导（该能力开启后另补 UI）。
- 底部「操作记录」入口：以所在资源过滤打开审计薄层（需该空间管理/可见审计权）。
- 底部「收起」按钮（记忆折叠态）；列表行 ·⋮· 的“详情”项再次展开。

> 注：详情栏内的每个操作仅代表“可达、可用”，最终裁决一律以后端判定为准。
- 空间内行亦作为 entry：从「共享空间」的卡片进入后，本 detail 栏行为与该空间文件一致(见前)。

### 4.11 共享空间视图
- 共享空间（部门盘/工作盘）进入前以 **SpaceSwitcher 集合页/顶部下拉**呈现“我可见的空间”列表（仅 `DRIVE_READ` 起的空间会出现在启动数据中）：
- 每条目以紧凑行/卡呈现：空间名称 + 类型 chip(`PERSONAL / FOLDER-DRIVE`) + 归属(部门/用户) + 成员数 + `已用/配额` 容量条 + 当前用户权限标签 + 「管理」徽(若是 owner/MANAGE)。点击名称进入其空间根文件界面。
- 空间根文件列表与个人文件共用同一组件，仅换数据 drive 参数；补充一个不可编辑的只读 badge `部门 · {部门名}` 于面包屑根。
- 成员聚合：空间头右提供「成员 (人数)」头像堆叠，点击展开气泡成员卡；owner/MANAGE 另有管理入口跳系统管辖成员/权限（见第五部分）。
- **权限不足控件原则**：无读权的空间不进入启动列表；进入空间后，对具体操作缺失的权限在相应控件灰显并 tooltip 说明，不一键把所有按钮隐藏以免反复报错误导“没这功能”。
- 「复制 / 移动」跨盘：进入对话框选择目标盘/目录（跨盘语义是否 OK 见待确认 C，MVP 若仅同盘则选盘控件受限并注明），单列 cross-drive 约束。

### 4.12 回收站页（独立回收管理）
- 列表列：名称(type icon)；原位置(回收前路径，可点击跳转到仍活动父目录)；删除时间；删除人；**剩余保留时间**(由 `trash_at` + 保留策略≈90 天推算，策略值来自系统配置不硬编码)，例如标「将在 12 天后自动清除」；大小。
- 操作（多行支持多选后批量）：
  - **恢复**：校验目标可写（恢复向目标需 `FILE_CREATE`，回收能力需求据修订 §12）。目标父已不存在时恢复到空间根；同名冲突则后端自动返 `原名(1)` 并以 toast 说明。父目录也在回收站的情形（整体删除）：MVP 只支持“整文件夹恢复”，会一并恢复祖先链与子树，操作文案注明「将连同其上级已删除目录一并还原」，不提供拆分到乱状态的半恢复。
  - **彻底删除(local purge)**：danger 危险弹窗明确「释放存储且不可恢复」；对整目录 purge 后端可能异步返回“已开始清理”，页面给出状态条可查看/关闭。purge 需要同盘文件删除/管理权（详细鉴权后端定，见 §11 映射行）。
  - **清空**本空间回收站：需 MANAGE，二次确认 danger。
- 空态：空回收站显示说明文案；恢复主按钮在无 `FILE_RESTORE`+目标写权时灰显并 tooltip 权限原因。
- 该页默认作用域=当前空间(个人盘/所在共享空间)。管理端视角的全平台回收仅用于治理，见 §5.8。

### 4.13 最近 & 收藏
- **最近**：以 Keyset 列出最近操作记录（记录来自后端“最近访问”能力）；行被点击即直达——文件行直接进入预览/定位页，目录行进入该目录根；前缀示所属空间与路径以便消歧。
- **收藏 ★**：作为导航根的收藏为**可持久化成员级数据**，技术上依赖后端 favorite（objId↔user + star 状态与列表接口）；修订方案尚未定义该表/接口——UI 层文档在此仅陈述“将呈现”，并把该依赖列作**待确认 B**：若后端未提供，则 MVP 不发布“收藏/最近”一级导航（避免伪造），以“我的文件 + 搜索/结构浏览”为入口，相关 IV 页 P1 相应顺延。
> 因此任何 UI 实现不得把收藏回退为“仅本地 localStorage 临时假收藏”来假装能力已具备——否则跨端不一致且误导用户。

### 4.14 全局搜索页
- 触发：点击 Header 搜索框或 `Ctrl/Cmd + K`，进入全屏搜索检索层（保留一层结果上下文，可返回原处）。
- 顶部(常驻)：搜索输入 + 「结果 N」+ 过滤 chip 带：**全部 / 文件夹 / 文档 / 图纸(dwg、dxf) / 3D模型(model类) / 图片 / 代码脚本 / PDF / 其他**——族划分取自 `constants/fileTypes`（§2.6 阵营，与后端检索类型过滤一一对应）。次级筛选：时间范围(今天/近7天/近30天/自定义)、空间范围（可选我可见的多盘 multi）。
- 结果区：默认列表、Keyset 滚动；每行=类型 icon + 名称(命中关键词片段高亮) + 所在目录路径串 + 空间 tag + 大小/时间/人；行操作提供「打开预览」。行内是否渲染文件缩略图取决于该类型是否具备离线可得的预览产物（列表/网格对应能力，见 §4.4 zoom）；`hover 放大预览窗为 M4 预留增强项`。对目录结果默认“进入目录定位”，对文件结果默认“预览”。
- **范围安全**：检索范围由后端限定在调用者有读权的 scope（修订 §13 搜索权限），客户端不做二次过滤以免泄露。
- Empty 态轻提示「未找到与 ‘xxx’ 相关的内容」+ 若干检索建议。回收站内容不参与全局检索。

### 4.15 在线预览页（统一 Preview，provider 无关）
- 统一 `FilePreview` 容器：把后端 `preview-info` 返回的 **kind**（`cad2d → view-kind 、model、pdf、image、code/text、video-hls、office-pdf、audio、none`）映射到对应的 viewer；UI 绝不绑定、显名任何转换内核（ODA/LibreDWG/assimp…）。渲染层只消费「产物 URL + 产物类型」，2D 消费 svg 面、3D 消费 glb 网格、PDF/pdf.js、图像/文本各自容器 ——引擎无关性在本页必须成立。
- 顶部工具条固定：左=返回文件目录；中=当前文件名（截断可 hover 全名）；右=下载按钮(仅 `FILE_DOWNLOAD`) + 文件信息(弹详情)。视图控制放视图区上方留白。
- 预览加载状态严格对后端：**queued / processing / ready / failed / offline / none**；前端只按返回为指定过渡文案。
  - `queued`/`processing`：居中细负载 spinner + 文案「正在生成预览」；MVP 前/前端轮询 `/file/preview-task/{jobId}`≈2s，就绪即载入。
  - `failed`：说明卡「暂时无法预览」+（若有下载权）「下载以本地查看」；当 source 仍在可提供「重新生成预览」触发，engine 就绪性留给后端。
  - `offline / none`：不支持该类型的说明页（如 office/音视频等未开放能力仅文案，不以“已支持”伪装）。
- **2D 图纸(dwg/dxf)**：主视区为可缩放 SVG/瓦片面。工具条（toolbar)= 缩放+−、适应屏幕、100%、全屏（MVP）；「图层」「测量」为 **M4 预留入口**——能力由 provider PoC 后才确定何时可用；不可时入口 disabled+tip「后续版本提供」，不伪装为现成能力。
- **3D 模型**：WebGL 视图加载 glb(Draco 就绪即可)。工具条=旋转、平移、缩放(滚轮/手势)、重置视角、线框(开关)、模型信息(顶点等——数据存在才显示，否则隐藏)、全屏；「剖切/截面」M4 预留，不可用 disabled。若内核当前无法产出 glb，UI 正常落入 failed 说明卡。
- **PDF**：pdf.js canvas 翻页视图 + 顶部页码/缩放/适应/宽度；**图片**：单幅适应屏、深灰观看底；**代码/文本/脚本**：只读高亮编辑器+行号+复制（复制与下载均以 `FILE_DOWNLOAD` 判定；无下载权则复制/下载禁用 & top 一句话说明原因）；超大文本按 backend 截断提示（前端给「大型文件仅显示开头」灰注），不强制下载全量。
- **office / 音视频 / audio**：云能力未开放前给出“……暂未开放预览，支持下载查看”说明页（按后端 kind），绝不展示无法工作的播放器占位。
- 全屏：桌面预览载入栈内，ESC/返回退出；移动端以原生栈进入预览页而非桌面式覆盖。多版本预览 MVP 仅当前版本(cur)，历史版本恢复预览不列入窗口（M4+）。

### 4.16 通知中心（顶栏铃，右下拉）
- 计划时间线式通知项：上传完成/失败、批量任务完成、审计类低等；未读数红点+置灰已读。
- 能力依赖：**后端 notice 中心接口/推送数据模型目前不在修订方案内**。MVP 达成方式=本地任务事件(由 uploader/async task store 驱动：上传/删除/恢复完成与失败)；「被加入空间/权限变更/系统公告」类需后端通知，先列 **待确认 E**，在收到后端前此类不强推；避免给用户空承诺“未读提醒”却无可靠耗源。

> 备注：本部分页面字段/能力最终以后端真实返回截断依据；页面中不确定能力统一以「见第十一部分待确认清单，按契约受后端能力 gating」处理，开发不臆造数据或“能看不可信”的通知。

---

## 第五部分 管理端核心页面逐页设计

> 管理端与用户端同 token/组件，只是一屏更密、更强表格、更强的过滤与操作。角色控制由后端断言（平台 admin 才能进入 admin 路由，按 §5.5 防越权)，前端只做拉入性收口与 UI 灰显解释原因，非安全边。

### 5.1 管理左栏结构
管理区（进入 admin 由角色：平台管理员/被授予部门管理的用户在可管辖范围内）分组为三块而非巨树：
- **运营**：概览 / 组织与成员 / 空间管理 / 空间成员权限（scope 到自己有 MANAGE 的空间）
- **治理**：文件管理（按空间浏览）/ 配额 / 回收站（按空间）
- **审计与系统**：审计日志 / 系统配置
默认进入「概览」。管理员入口不级联展开成漫天菜单，而是每页次级 tab 主导内部细节。

### 5.2 管理概览 AdminOverview（少而精，拒绝大屏化）
- **号条（4 个 numstat，非卡片墙）**：用户总数 / 部门数 / 空间数 / 总配额。
- 次要行展示容量：`已使用(used)｜ 预留(reserved)｜ 可用`，(仍明确三态，不让一个百分比吞掉 reserved)。
- 中部分两栏：左「空间用量 Top」按 drive 排序进条表（hover 可见 used/reserved/quota 精确值）；右「近期活动」最新操作名鉴（来自审计）。
- 图形少而精：可含近 7 日上传量趋势(细线+淡区域)与文件类型占比单选一项即可；其余一律进次级页，不为撑版面硬加图。
- 告警（异步任务/预览失败、密钥类配置到期）列入独立提示行；管理区左栏路由有则红点。
- 数据全部来自聚合/分页接口（§20 `GET /api/admin/runtime`+分页列表），首屏不做多重长请求；若需“趋势/类型分布”而 runtime 未含，宁可收敛为“仅总量”并记为后端待加项(I)而非伪造展示。

### 5.3 组织与成员
- 左「部门树」(lazy 展开、每节点人数)，右「成员表」：列=姓名 / 部门 / 手机 / 状态(在职、停用) / 角色(平台/部门管理员 tag、成员) / 空间数 / 最近登录 / 管理；支持文本搜索 + 状态过滤。
- 添加成员：从钉钉同步到的用户中挑选加入（无需另行自助注册）；创建者即自动获得该空间预设角色由管理员按 §5.4 建盘时初始化。
- 成员详情抽屉：基本信息 + 所属部门 + 云身份 + 名下空间列表 + 可管理的空间 + 账户状态（停用/重置密码/重置鉴权）。
- 顶层按钮「设平台/部门管理员」：多级授权仅限平台 admin，且不能给自己升级、不能改同级 (修订 §5.5 后端强制)；危险动作给足确认文案。停用用户：UI 文案说明连锁——其 token 立即失效（技术 §4.3/token_version）、其所属空间成员关系不影响他人；二次确认 danger，后端判定状态。

### 5.4 空间管理（drives-admin）
- 表：空间名 + 类型(`PERSONAL / FOLDER-DRIVE`) + owner(人/部门) + 成员数 + `used / quota`(容量条) + 状态(启用/停用) + 创建时间 + 行 ·⋮·。
- 新建空间（选 personal 绑定用户 / 建部门盘绑定部门、设置初始配额）：创建后进入初始化成员（部门盘由管理员按 §5.4 一次性把部门管理者置为 `MANAGE` 成员）。停用空间仅读拒写 (修订 §6/17)；UI 状态 toggle 带说明文案「停用后将拒绝新的写入/上传，读取与下载仍可」。
- 行点开空间详情页（或右侧抽屉）：段=基础信息 | 成员 | 配额 | 回收站(该空间独立恢复/清理链路) | 操作痕迹(对此空间审计过滤)。**owner 移交 transfer** 位于详情管理危险区，danger 确认（仅平台 admin）。
- 改配额弹窗客户端校验：新 quota ≥ used + reserved，否则即时红字提示不可保存。

### 5.5 空间成员权限页（管理员核心；克制，不用 checkbox 墙）
- 布局为「左成员列 + 右权限编辑」两栏；权限编辑**一次一个成员**，能扫读。
  （成员列表支持搜索/勾多个→右上“批量…仅可批量应用某条快捷档” ，见下。）
- 顶部**快捷预设**仅是编辑器辅助，不新增聚合权限语义：点某预设=后台一次性替编辑器勾好对应细码集**而这些码正是后端持久化的精确码（§5.2 扁平集）**；预设仅为方便，不做“预设”本身入库：
  - `仅查看` = {DRIVE_READ}
  - `浏览+预览+下载` = {DRIVE_READ, FILE_PREVIEW, FILE_DOWNLOAD}
  - `协作者(读写内容)` = 上 + {FILE_CREATE, FILE_WRITE, FILE_MOVE_COPY}
  - `内容全权(可删)` = 上 + {FILE_DELETE}
  - `空间管理` 不包含于预设，单独 MANAGE 开关管理。
- 细粒度区用能力行（浏览/预览/下载/上传新建/重命名改内容/移动复制/删除/恢复管辖）逐 checkbox；UI 呈现**依赖**：勾预览→自动含浏览(`FILE_PREVIEW⊃DRIVE_READ`)；禁止“只勾下载缺预览”的非法组合（灰禁+tooltip）。UI 的这些隐含仅在**界面**帮人避免错误，存储与后端一致的精确平码（不生产“隐含层”字段）。
- 单独分组「空间管理者 / owner / transfer」放 danger 区：owner 不可降至低于只读/不可移除；MANAGE 开关与权限变更可能有互斥同级约束由后端复核，前端据返回置灰。
- **全页面统一次交互**：权限编辑器为「显式保存」——改任意 checkbox 后点“保存”一次写入（put perms），不逐 checkbox 请求；保存成功即时生效（权限不在 token）并 toast。批量为多成员允许一次点“设置权限”应用同一档。

### 5.6 配额管理 Quota
- 展示强制体现 **三态（used / reserved / available）**，不只给一个百分比：条上 `已用 reserved 已预留`（used 主蓝 + 紧跟其后的 reserved 展示为琥珀窄格、可用为浅灰余白），三段总和=quota。平台汇总与单空间明细沿用同一三态条图。
- 设置各空间默认配额策略(个人盘默认 Profile、部门盘默认)并存 `storage_config`（修订 tb_storage_config），支持全局默认覆盖个别空间。
- 表=各空间 + reserved/used 条图 + quota 数值列 + 修改入口；配额超支为 danger tag。「配额告警阈值」与占满动作视后续需要，均非 MVP 默认项。

### 5.7 审计日志页
- 过滤行：时间范围 / 用户 / 部门 / 操作类型(action 枚举来自 §19 动作集) / 资源类型 / 结果(success/fail) / 空间；结果表 Keyset 分页滚动。
- 列=时间 / 用户 / 部门 / 操作(本地友好文案 + 原始枚举tooltip) / 资源名称 / 资源类型 / IP / 结果(成功绿·失败红)。失败行 hover 展示 `error_code`。「查看详情」Modal 读该行 `detail_json` 前 pretty JSON 只读展示 + 拷贝。(导出能力后需 check——修订未给导出接口，MVP 面板只读展示;待 G)。
- 权限 scope：平台 admin 全量；职能部门 admin 仅其辖空间层面（后端 scope），UI 过滤值同步后端可见范围，不做本地二次猜测。

### 5.8 文件 / 回收站（admin 治理视角）
- 「文件管理」定位为治理页而非“代删”：以选择某空间为 range，浏览/检索该空间文件、定位超大或异常项；超限文件清理统一引导到该空间的回收操作路径，保证可逆，避免 admin 误触跨空间删除。不暴露无关空间的全局对象删除按钮。
- 「回收站」全局视图按空间划分为标签页分别展示恢复/清理，各空间仍沿用 owner/MANAGE 的回收权限判定；安全 MVP 以当前有管辖权的空间为范围，绝不因“平台 admin”字样而去删不属于管辖的盘（防越过 §5.3 的隔离语义）。

### 5.9 系统配置
- 基于后端 `storage_config` 键值以**分组表单**呈现（OSS 地区/inter/端点、钉钉 Agent、上传分片阈值与并发、回收站保留天数、preview worker 心跳超时等），键与类型(数字/bool/secret)声明驱动渲染。
- **secret 字段仅可“覆盖新值”，绝不回显原值**；保存前后端各自校验+写审计。集成动作（触发钉钉同步 POST /admin/org/sync）独立提供按钮并显示执行状态。
- 凭据字段均遮罩(look password)。

> 管理端各页 "图表/列表密度"符合企业;危险区(删除/停用/transfer/清空回收/覆盖改权限)统一 danger red + 二次确认 dialog(inline textual)。

---

## 第六部分 移动端适配策略

> 目标：移动端不是 PC 缩小。多端差异一律走 `adapters/` + uni 平台条件编译封装；保留 PC 完整链路，移动端按能力裁剪且绝不出现"能看不能操作"的误导死摆。

### 6.1 Tablet
- 视图沿用桌面宽版面，但侧栏于宽度≤1100 自动折叠为 64px 图标态（点汉堡展开覆盖层）；文件列表沿用列表(紧凑行)。触板不支持拖拽——页面一律不提示“拖到此处”，改用行动单/移动对话框。

### 6.2 Mobile（H5 / 微信小程序 / App）
- 全局以底部 TabBar(3 区内置居中“上传”主钮)主导航：`文件 · + · 搜索 · 我的`；“共享空间/回收站/最近…”等二级集合不再塞满底栏，而是文件根页首行的分段 chip/抽屉入口即时切换——底栏只承担高频出口，导航不臃肿。
- 文件根默认“我的盘(个人)”；顶部面包屑/下拉负责在个人盘与共享空间之间切换。
- 桌面“鼠标/右键”整套不照搬：
  - 长按(≥500ms)对象呼出行动单（预览/下载/移动/复制/重命名/详情/删除）；
  - 精简入口：行右侧“⋯”弹出行动单（预览/下载/移动/复制/重命名/详情/删除）；
  - 多选低频：左上「选择」起选择模式→出现行勾选 + 底部批量操作条(下载/移动/删除/返回)。
- 文件列表移动端列少：icon(40) + 名称(主行) 与一行聚合元信息(时间·人)。左侧勾选列仅在“选择模式”出现；默认以列表表达(信息多)而非宽格；底部 Sheet 抽屉(从下弹)而非右侧浮动详情。
- 上传：文件/文件夹选择能力受 iOS 授权与微信小程序限制，一律走 `adapters/`；无拖拽；上传任务不做桌面式右下浮窗盖界面，改为**顶栏常驻窄条胶囊 `上传中 N · 共 X`**，点按拉起上传面板/挂后台。
- 管理页（member/权限/配额/回收）移动端不要求等高排版：可表格内容以 List 抽屉化、权限页用 抽屉内全屏编辑区（成员在上、权限块在下）在容器内纵向滚动；默认仍以 PC(≥iPad)为主屏，手机为尽力适配。
- 预览：原生栈进入全屏 `Preview` 页(PDF/图像/文本直读顺畅)。3D(GLB)：H5 桌面由 WebGL 能力满足；小程序端若无 WebGL，预览页降级为“当前设备不支持，请在浏览器/桌面查看”，并给下载——绝不装作能旋转而实为死图。
- 下载/打开：命中平台能力以系统打开/另存；被下载权限限制时的按钮禁用及提示逻辑各处一致。

### 6.3 adapter 能力清单（多端差异点，经条件编译封装）
| 能力 | H5 桌面 | H5 移动 | 微信小程序 | App |
|---|---|---|---|---|
| 列表视觉主 | 列表(+可切网格) | 列表简化 | 列表简化 | 列表简化 |
| 拖拽 OS 上传 | ✅ | ✖ | ✖ | ✖ |
| 网盘内拖拽移动 | ✅ | ✖(改用行动单/移动到) | ✖ | ✖ |
| 右键菜单 | ✅ | 长按 Popover | 长按 | 长按 |
| CAD2D/文本/PDF/图预览 | ✅ | ✅(web) | 按能力 | 按能力 |
| 3D(GLB) WebGL 预览 | ✅ | 视 H5 webGL | 受限→桌面提示 | 按 adapter/桥能力 |
| 选文件夹(上传目录) | webkitdirectory | 受限 | 受限 | 受限(按 UTS 桥现状) |

---

## 第七部分 前端组件架构

> 组件分类明确：`C`=通用跨端、`U`=用户端业务、`A`=管理端业务、`R/M4` = 预留（后端或内核能力未定前负责以 「能力开关 stub」呈现 safe），特殊通用组件经 UI 复用逐项引用实现与页面。
> 组件职责、入参(面向 data/contract)避免业务内联写死權限。路由相关各目录的单一组件统一并都连 stores 根 (`useDrive/filt`) 共享 Session。

### 7.1 布局壳

| 组件 | 分类 | 职责 |
|---|---|---|
| `AppLayout` | C | Header + 侧栏 + 主区骨架；children 按路由注入 |
| `TopHeader` | U/A | Logo + 全局搜索 + 上传入口 + 通知 + 头像菜单；由页面配置决定哪些模块渲染（admin 复用同壳） |
| `SideNavigation` | C | 导航注入（mode=user/admin），支持折叠（216/64）与胶囊收起 |
| `StorageUsage` | U/A | 空间容量（used 主色 + reserved 琥珀窄区叠加），读容量汇总数据 |

### 7.2 文件浏览核心

| 组件 | 分类 | 职责 |
|---|---|---|
| `FileToolbar` | C | 上下文工具栏（无选择/单选/多选三态；按钮可用性由权限码计算） |
| `FileBreadcrumb` | C | 面包屑 + 层级路径（入口指向当前 drive 根） |
| `FileList` / `FileRow` | C（U/A 复用） | 列表列定义 + 行状态（选中/悬停/禁用） |
| `FileGrid` / `FileCard` | U | 网格 + 缩略 / 类型图标兜底 |
| `FileContextMenu` | C | 右键菜单：由动作数据集（每项携带所需权限）供渲染与可用性计算 |
| `DragOverlay`/`DropZone`/`DropTargetFolder` | C（仅 H5 #ifdef） | 拖拽视觉层 + 放置检测（语义见 §4.8） |
| `NameInput` / `RenameDialog` | C | 就地改名输入 / 弹层重命名（含冲突提示） |
| `FolderPicker` | U | 目标盘/目录选择（移动/复制对话框，跨盘约束见 §4.8） |

### 7.3 上传侧

| 组件 | 分类 | 职责 |
|---|---|---|
| `FileUploader`（controller） | C | 文件 → 队列引擎：桌面/浏览器差异在 adapter 收敛；controller 仅持于 store，不做 DOM 层状态 |
| `UploadTaskCenter` / `UploadTaskItem` | C | 任务面板与单个任务行（映射 §4.6 状态）；内建迷你胶囊变体 |
| `UploadConflictDialog` | C | 同名冲突决策（对应 §4.7，语义写回上传参数） |
| `FileSelect` / `DirSelect`（adapter 桥） | C | 平台文件/目录选择器统一入口 |

### 7.4 详情信息 & 对象元数据

| 组件 | 分类 | 职责 |
|---|---|---|
| `FileDetailPanel` | U | 右侧详情栏（可折叠，见 §4.10） |
| `VersionList` | U | 历史版本网格/列表；不具版本能力时隐藏入口，单文件对象提供 |
| `SpaceSwitcher` | U | 空间覆盖视图列表（展示我可见空间及容量/我的权限），跨空间入口 |
| `PermBadgeList` / `MemberAvatarStack` | U | 详情栏“我的权限”chip / 空间成员头像堆叠 |

### 7.5 预览

| 组件 | 分类 | 职责 |
|---|---|---|
| `PreviewLayout`（shell） | C/R-M4 | 通用容器按状态渲染 queued/processing/failed/offline/none |
| `PreviewStatusBar` | C | 加载提示 / failed 说明 / offline 文案，对接后端状态 |
| `Cad2dViewer` | R-M4（provider 无关） | 消费后端产出的 2D 产物（svg/面），不引用任何内核名；能力不足走 `PreviewFailed` |
| `ModelViewer` | R-M4 | WebGL 载入可得的 glb；能力不足同前 |
| `PdfViewer` / `ImageViewer` / `CodeViewer`（只读）/ `TextViewer` | C/U | 各自只读容器 |
| `VideoViewer` / `AudioViewer` | R | 云能力开启后接入，未开启则统一走说明页 |

> 预览 viewer 路由由 `kind → viewer` 的注册表映射完成（见 constants `previewKinds`），保证引擎无关、可扩展。

### 7.6 列表与通用元素

`GlobalSearch`（入口可覆盖顶栏展开）、`RecycleBin`（列数据 + 批量操作）、以及 `EmptyState` / `ErrorState` / `LoadingState` / `ConfirmDialog` / `Popover` / `Menu` / `Toast` / `Skeleton` / `Badge` 等为一套跨端通用组件。

### 7.7 管理专用

- `MemberSelector`、`PermissionEditor`（见 §5.5，持久化存储为扁平权码集）、`QuickPermPreset`（快捷预设 chips）、`QuotaPanel`（三态条）、`AuditTable`、`AuditDetailModal`（pretty JSON 只读）、`SpaceMemberManager`、`SpaceTableRow` 等置于 `components/admin/`；用户端不直接引用。
- 权限相关数据组件均做成**声明式**：标注 `required-perm`，内部据会话权限渲染 only `can(code)` 决定可用/禁用，权限判定收敛单一来源。
- > M4 预留 viewer 采用统一能力探测 + 安全回落：当后端/内核暂不具备某格式 → `PreviewFailed` +「下载以本地查看」，绝不伪装为就绪。能力集中维护在 set（`capabilities` 常量）中。

---

## 第八部分 交互状态规范

### 8.1 通用元素视觉态

| 状态 | 规则 |
|---|---|
| hover | 文件行表面 hover 底（`surface-hover`）；文件行图标不变；可点元素文字/图标转主色并小幅（禁用大位移） |
| active/按下 | 按钮用主色 active 色；行按下用 `surface-selected` 淡底 |
| selected | 行 `surface-selected` + 行首 2px 主色竖条；所选名称/图标可用主色强调，不做高饱和背景 |
| disabled | 前景降低对比（0.45）+ `not-allowed` 光标；因缺权限而禁用的项额外给 tooltip 解释原因 |
| loading | 细线 spinner + 文案；避免整页转圈，首载用骨架行；按钮加载中禁用；上传整体状态见 §4.6 |
| empty | `EmptyState`：中性图标 + 一句文案（按“可写/只读”两模板），轻量主行动作 |
| error | 危险图标 + 提示与重试；服务类错误用顶部细条；输入字段用内联 error 红 + 说明文字 |
| success | 组色反馈（极短 toast ≤2s/单条表面态），无夸张动效；冲突类给独立处理卡 §4.7 |

### 8.2 文件操作交互

| 操作 | PC | 规范/对应能力 |
|---|---|---|
| 单击 | 单选（常态保持行可辨识）；悬停行浮出操作 `⋯` |
| 双击 | 目录=进入；文件（有 `FILE_PREVIEW`）打开统一预览（§4.15） |
| 单击名称 | 名称进入就地编辑（select-in-name），用于改名 |
| 右键 | 打开 `FileContextMenu`（对象行或空白区两种），可用性按 `can(...)` 计算 |
| 键盘 | 全局快捷键统一拦截（文本输入框内除外），采用可控的按键绑定层 |
| | `Ctrl/Cmd+A` 全选当前目录；`Ctrl/Cmd+C/X/V` 站内复制/剪切/粘贴（经 clipboard store，不触碰系统剪贴板），权限在动作时再校验 |
| | `Delete`＝移入回收站（见 §回收）；**不提供** shift+Delete 直接销毁：技术未定义永久删除的桌面快捷能力（避免丢失），永久删除限定在回收站内并二次确认 → 见待确认 K |
| | `F2` 重命名（无 `FILE_WRITE` 时禁用并提示） |
| | `Enter` 打开/进入；`Escape` 关闭面板或取消选择；方向键移动选择 |
| | `/` 聚焦全局搜索（纯键盘用户上下文） |
| | `Alt+←`/`Alt+→` 浏览器栈前进/后退（与行历史一致） |
| 拖拽 | 放置目标/禁止表达返回 §4.8；触屏不启拖拽交互 |
| 多选 | 顶部工具栏切换为“批量”语义并显示所选计数（移动端用底部操作条） |
| 反馈 | 上传成功/失败保持统一任务条呈现；单行级失败的即时信息在对应行旁提示，禁用静默失败或者静默“成功” |

### 8.3 通用规则

- 可感知过渡即时（hover ≤ 80ms），面板淡入约 120ms，浮层出现无缩放。
- 菜单/搜索浮层点击外部关闭，并保留输入焦点回环；动作建议仅靠悬停，不劫持焦点。
- 数据一致性：权限在会话中的变化（被降权）会让正在排队的批量动作失败——必须给出统一次失败提示并触发刷新权限缓存，不静默吞掉。
- 无障碍：控件可聚焦、标签/光标可见（aria），颜色不作为状态区分的唯一手段。
- 单位与数字格式统一在 `utils/format` 单一来源（大小按 1000 进制：kB/MB/GB，与后端一致），页面不各自换算。
- 所有动作最终权限判定以后端为准；界面灰显 + 可解释原因，不做危险隐藏式的“假装无此操作”。

> 交互双后验：一切安全关键闸不可只在 UI(禁用可绕过)：UI仅降低"看起来可做"；后置 `@RequireDrivePerm`。UI guidelines 页会展示 "视觉可做 ≠ 允许"(e.g. export能力)，让开发不至前端校验当鉴权做。

---

## 第九部分 前端工程目录与模块职责

> 以 uni-app + Vue3(Vite) + pinia 构建。目录按「用户端/管理端业务隔离、通用上收一级」组织；页面与后端接口 1:1 归 `api/modules`。

```
src/
├─ main.* / App.vue                      启动、全局 store 装配
├─ styles/                                tokens(uni.scss·vars) + 排版 + 通用
│   ├─ tokens/_color/_type/_space/_radius/_elev/_layout
│   └─ mixins/_elev…(icons)
├─ constants/
│   ├─ perms.js                 // §5.2 全量码字面（杜绝手写散权限）→ 集中
│   ├─ fileTypes.js             // 扩展名→family（folder/image/pdf/doc/xls/ppt/caddwg/caddxf/model/…）单一映射源
│   ├─ previewKinds.js          // 产物 kind → 显示文本/文案映射（对应预览 kind 枚举）
│   ├─ uploadStates.js          // 后端 task 状态 → 前端展示文案映射
│   ├─ texts.js                 // 通用展示文案/空态（策略性提示集中）
│   └─ adminActions.js(审计 action 标签)
├─ utils/
│   ├─ logger request-id/format util(size/date/…) 
│   ├─ debounce/idemKey(uuid)
├─ api/                            // 与后端 path 对齐；统一 request 实例+刷新拦截+ReqId
│   ├─ request.js                  // base/jwt/refresh/错误归一/X-Idempotency-Key(写)
│   ├─ auth/organization/me        // me, perms, drives
│   ├─ drive.js                    // drives/ drive members/perms映射(管理admin时)
│   ├─ file.js / upload.js / trash.js / versions.js / search.js
│   ├─ preview.js / audit.js / config.js / runtime.js(admin)
├─ stores/                        // pinia
│   ├─ session(me nav meta)
│   ├─ perm(cache drive perms + isOwner/MANAGE + refresh/evict)
│   ├─ driveList(current drive/record)
│   ├─ fileView(sort/filter/cursor, path+root stack)
│   ├─ selection, clipboard(站点 move/copy临时)
│   ├─ uploader(全局队列 persistent by tasklist) 
│   ├─ trash, search, notifications(local-only/MVP)…
├─ composables/
│   ├─ useDrivePerm / useObjects(keyset loader) / usePreviewStatus(poll)/ useUploadTo / useDropTarget(H5)…
└─ adapters/                      // 多端差异封装（#ifdef）：文件选择/目录递归/断点/3D能力探测；与预览 provider 无关
│   platform adapter (web/app/mp) → upload/filesystem/webgl gate…
├─ layouts/                       // 工作台/管理台壳
│   ├─ AppLayout.vue, AdminLayout.vue, FileShell 组合 header/sidebar/storage-usage
├─ components/
│   ├─ common/                    // font/icons(type file map)/menu/toast/modal/skeleton/badge/empty/loading …
│   ├─ file/                      // toolbar / crumb / list / grid / row / context / detail / version / drop
│   ├─ upload/ …/ trash/ search/ preview/(shell + 按 kind 的 viewers)
│   └─ admin/                     // org / drive / member / permission / quota / audit / runtime…
├─ pages/                         // uni 原生页面，按 modules 分包（见 pages.json）
│   ├─ user/                      // files / spaces / recent/starred / trash / search / share / preview …
│   │                             每页单向依赖 store + api + composable；权限码只引自 constants.perms
│   └─ admin/                     // overview / org / drives / members / quota / audit / config …
└─ (uni pages.json 负责 tabBar、分包与页面栈声明；路由内置，不需 router 库)
```
职责边界：
- 用户端 `page 分包(user)` 与 `admin` 分开（`pages.json` 分包加载，避免互相耦合、减首屏）。
- **权限字面不散在页面**：统一由 `constants/perms` 集中导出；页面模板只经 `can(FILE_DOWNLOAD)` 之类判断渲染。
- `api` 层仅做接口封装(请求/链错误归一/鉴权/字段映射)，不得混入业务 if。文件类型映射只存在于 `constants/fileTypes`（视图消费之）。
- 预览能力不强耦合到客户端内置清单：`kind → viewer` 注册表可扩展(`previewKinds`)，使未来 provider/M4+ 新增格式时只加映射不动引擎代码，保持内核中立。

---

## 第十部分 页面开发优先级

> PC 桌面 UX = 第一优先;移动/管理低优并行不在桌面 preempt。
> P0=文件清单MVP开关页面(onion)：用户端文件浏览主路径 +上传状态中心 + 预览统一框架。
如后 P1/P2 按依赖列出预。
| 优先级 | 目标 | 用户测试用例核心 | 对应§ |
|---|---|---|---|
| P0 | AppLayout+Header/Sidebar/容量、面包屑、文件列表(list) Keyset+空/加载/权限-error、右键菜单、多选、改名/删除入回收、版本列表简单 | 员工日常浏览下载 | §4.1-4.5/8 |
| P0 | FileToolbar / 上传(单片+multipart+断点续 resume/cancel/pause)+UploadTaskCenter+冲突(同名仅加写/覆盖 by FILE_WRITE)+upload至/覆盖版本 | 拖/点传成功、失败task visible、断点重连续 | §4.6-4.7/9 状态 |
| P0 | Preview shell(queued/processing/ready/failed/offline)按 kind 的框架映射(为 PDF/image/text/Code即可 - 其view用现 readyMVP; cad/3d hooks 4.8预留内核) | 图/pd/f代码直开;若 dwg 未 Ready 则 queued→成功待PoC | §4.15 |
| P0 | 回收站(恢复/彻底删除/purge给 secondary、保留时间) | delete入回收→恢复/删 | §4.12 |
| P0 | 共享空间 Switch view(basic SpaceSwitcher 进 drive member),跨drive权限禁用灰(semantic) | 有多 drive读到只读态 | §4.11 |
| P1 | Search 全局(表单/筛选type/keyset列表)、最近/收藏(need #BT favorite endpoint) |  搜索即 find | §4.13-4.14 |
| P1 | DragOS+inner drop 双向+|Grid | 拖传/拖移动/禁放 | §4.8 |
| P1 | FileDetail 展开+fold (含版本管理+MD5 依download権) | 侧栏信息 | §4.10/8 |
| P1 | admin 骨干:概览/组织/space members perms/配额 (只读先读/后才写) 每驱动文件与回收 scope | managers查重/查权限 | §5.2,5.4-5.7 |
| P2 | admin 详情写操作:audit/details动作、config 仪表+三态图优化、多区回收 purge 治理 | | |
| P2 | 移动适配细节 (替换抽屉/自底 sheet复用原生) | 手机上全职责链路走通 | §6 |
| (M4/R) Viewer CAD2D/ModelWebGL真实内核接入待Poc | M4 milestone gating，UI 框架已定 | 样片旋转 | |

> 打包必须保证任一 P 可独立可交付测试(不需等它以前) UI 设计迭代快。

---

## 第十一部分 页面清单 ↔ 功能 ↔ 后端接口 ↔ 所需权限码对应表

> 权限码严格引用《修订》§5.2(drive-level)与 §5.5 (auth级别/超管)，此处管理"操作钮可见/可用的归因依据码"(后端权威)。标 R=由后端能力开关控制不对用户暴露。待确认行 →「UI-T 待确认」批注见尾部。`*`＝路径方法按《十七》保持一致。

### 11.1 认证/全局
| 页面/组件 | 功能 | 后端接口(《十七》) | UI 需要的权限/角色门 |
|---|---|---|---|
| 启动/守卫 | 拉我的会话+可介入可见空间 | GET /api/me; GET /api/drives | 鉴权后(其余无) |
| 登入接钉钉 | 跳钉钉/ 免登 code | POST /api/auth/dingtalk | (免登) |
| logout/refresh | | POST auth/logout, refresh | JWT+token_version 时后端处理 |
| 我的头像菜单 | 管理入口可见性 | 导航角色(由/me role/is_super…) | is_super_admin或 authorized role |

### 11.2 用户端文件主界面（P0 列）
| 页面组件 | 功能 | 后端接口 | 权限码(说明在提示灰显用) |
|---|---|---|---|
| 文件列表/目录 | 列目录/分页 Keyset 排序筛选 | GET `/api/file/list?drive&parent&...` | DRIVE_READ → 可见列表主体 |
| 新建文件夹 | 新建 | POST `/api/file/mkdir` | FILE_CREATE |
| 上传(+上传中心&断点) | 上传任务 | POST upload-token; sync/resume/pause/cancel/complete; quick md5 | FILE_CREATE(新) ; cover(覆盖现有/同名)需 FILE_WRITE；若现同名时后端给 decision,前端入 §4.7 |
| 上传任务面板历史/可恢复 | | GET `/api/file/upload/tasks`(owner) | (task owner;不列权限但普通进入只能 owner scope) |
| 重命名 | rename | PUT `/api/file/rename` | FILE_WRITE；若命中同名冲突由后端返回并在 UI 走 §4.7 改名交互 |
| 移动/复制 | move/copy | POST `/api/file/move`、`/api/file/copy` | 移动：源需 DRIVE_READ + FILE_MOVE_COPY，且目标目录需可写（FILE_CREATE 插入语义）；复制：源 DRIVE_READ + 目标 FILE_CREATE。UI 据双端判定可用性，无单一“全局可移”令牌 |
| 删除入回收 | delete | POST `/api/file/delete` | FILE_DELETE；删除语义=移入回收站，非直接销毁 |
| 版本列表 | 历史版本 | GET `/api/file/{id}/versions` | DRIVE_READ（浏览）；是否为可读需 FILE_PREVIEW 由后端 gate，UI 按返回渲染 |
| 恢复历史版本 | restore | POST `/api/file/{id}/versions/{vid}/restore` | 显示需 FILE_RESTORE；因“恢复”会产生新版本对象，可能同时要求 FILE_WRITE——最终以 §5.2/§17 判定映射（见待确认 F） |
| 删除历史版本（非当前） | 删历史版本 | DELETE `/api/file/versions/{id}` | 后端限制 owner / MANAGE；UI 此纽仅供该类角色，其余禁用 + tip |
| 下载 URL | download | POST `/api/file/{id}/download-url` | FILE_DOWNLOAD |
| 网格/详情缩略 | thumb | 产物地址 `/t/{hash}`（与预览共用产物体系） | 仅用户具备 FILE_PREVIEW 才预加载缩略；否则回退类型图标（不发起无权请求，见待确认 D） |

### 11.3 回收站 （作用域=当前空间；管理视角见 §5.8/§11.6）

| 页面动作 | 功能 | 后端接口 | 权限码（前端灰显依据） |
|---|---|---|---|
| 恢复 | 还原项 | POST `/api/trash/{id}/restore` | FILE_RESTORE + 目标目录可写（FILE_CREATE）；目标父已删时回归到空间根，同名由后端自动 `原名(1)` |
| 彻底删除 | purge | POST `/api/trash/{id}/purge` | FILE_DELETE（对象删除权，owner/MANAGE 细项由后端复核）；危险二次确认 |
| 清空本空间回收站 | 清空 | POST `/api/trash/empty` | MANAGE；后端异步执行后返回状态条 |

> 直接删除仅限在回收站内执行；桌面“列表 Delete”一律等同于移入回收站（对应修订 §12），不提供从列表直接销毁的入口（见待确认 K）。

### 11.4 收藏/最近/搜索

- **最近 / 收藏**：修订方案的 IA 在导航中有这两个根，但其数据表与接口尚未定义（favorite-by-user / recent-access 均未出现于《修订》内）。UI 不臆造对应接口；按 **待确认 B** 处理——若后端不提供，则 MVP 隐藏“最近/收藏”一级导航，以“我的文档 + 全局搜索 + 结构浏览”作为入口，避免出现死的导航根。页面设计仅在契约补齐后开放。
- **全局搜索**：检索与列表共用同一套 Keyset 分页协议：请求带 `${query} + ?drive=（可多值，限调用者有读权的盘，前端只读该范围） + 时间范围 + 类型组过滤`。类型过滤组由 `constants/fileTypes` 家族映射给出并可多族；后端做过滤（§13 检索权限），客户端不做越权二次过滤。权限：检索仅在用户有 `DRIVE_READ` 的空间范围内进行，无须额外“检索权限”。
- **空间成员列表的可见性**：普通共享空间成员**不**被给到成员列表入口；仅 owner/MANAGE 经 `/api/admin/drives/{id}/members` 可见并管理（对应 §4.11/§5.5 的权限门）。

### 11.5 预览页链路

| 页面动作 | 功能 | 后端接口 | 权限码 |
|---|---|---|---|
| 拿产物信息 | kind + 产物地址 | GET `/api/file/{id}/preview-info` | FILE_PREVIEW |
| 轮询任务 | 就绪探测 | GET `/api/file/preview-task/{jobId}` | FILE_PREVIEW |
| 下载按钮 | 下载 | POST `/api/file/{id}/download-url` | FILE_DOWNLOAD（见 §11.2） |

> 后端 `kind=none/offline` 且无产物时，预览页以 `PreviewFailed` 呈现“暂不支持该类型/离线”，不给“假可下载”按钮；仅具备 `FILE_DOWNLOAD` 才显示下载。2D/3D viewer 的启用由后端 kind 决定并自动路由，UI 不预设某引擎可用。

### 11.6 管理端操作 ↔ 接口

（卷针列权限按后端 §5.5 角色层判定；此处给出前端可用性口径与契约缺口。）

| 管理动作 | 页面 | 后端接口（《修订》十七） | UI 可用性口径 / 说明 |
|---|---|---|---|
| 概览聚合 stats | AdminOverview | GET `/api/admin/runtime`(17.7 等部分) | 首聚合所需 drive 容量经此返回；若 runtime 不含趋势/类型分布，管理概览先收敛为仅呈现聚合统计，不要为此臆造专图接口 → 趋势类列为后端补充项（见待确认 I） |
| 组织结构树（lazy） | 5.3 / 5.4 | GET `/api/admin/depts/tree` | 平台 admin / 被授权部门管理员 |
| 组织成员列表 | 5.3 | 需机构用户列表接口（`/api/admin/user/list` 是否提供） | 修订未明确独立 user list：若无则从部门成员聚合；契约缺口标记待确认，勿用“部门成员循环”硬顶页面 |
| 建盘/改盘（含名称、初始配额） | 5.4 | POST/PUT `/api/admin/drives/{id}` | 平台 admin；详情修改（可见性/配额）按 MANAGE |
| 空间成员列表 | 5.5 | GET `/api/admin/drives/{id}/members` | MANAGE（仅管辖范围可见） |
| 写成员权限（扁平码集） | 5.5 | PUT `/api/admin/drives/{id}/members/{uid}/perms` | MANAGE；且受 §5.5 不可超设 / owner 保护约束 |
| 设空间管理员（manage 标志） | 5.5 | PUT `.../members/{uid}/manage` | 仅 owner 可授予；不得同级互授，防超设以后端为准 |
| 移除成员 / 转移 owner | 5.4/5.5 | DELETE `.../members/{uid}`；owner 转让 PUT transfer | MANAGE（owner 自身不被移除）；owner 转让仅平台 admin（危险区二次确认） |
| 改配额数字 | 5.6 | 与建盘一致 PUT `/api/admin/drives/{id}` | MANAGE 进行单盘配额；全局默认配额写 `storage_config` 走系统配置 |
| 审计查询 | 5.7 | GET `/api/admin/audit?...` | 平台 admin（部门管理员按其辖范围 scope） |
| 审计详情 modal | 5.7 | 行内 `detail_json`（或在有需要时按行另取） | 契约缺口待确认 G：以列表行 payload 是否存在明细为准，不虚构详情二次接口 |
| 全局回收站治理 | 5.8 | 复用 §11.3 按空间（params 带 drive scope）；无专用 global 接口则以逐空间刻度呈现 | admin 仅能操作其有 MANAGE 的空间，杜绝“平台字样越权删” |
| 系统配置（K-V） | 5.9 | `storage_config`；钉钉同步 POST `/api/admin/org/sync` | 后端须暴露 config GET/PUT（修订 §17 未含，标注待确认需补充）；配置表单声明式渲染 |
| 通知中心等 UI 依赖的通用后端能力 | §4.16 | 无修订定义 | 归入待确认 B/E；未确认前相关导航/模块以可用性受限呈现 |

> 后端不含的能力 → UI 一律以“可用性受后端能力 gating”呈现（禁用或该节隐藏），绝不形成点下去必失败的“死按钮”。

## UI 与技术方案待确认项（仅记录，不改已拍板方案）

> 以下条目是 UI 设计过程中识别到的**技术契约缺口/取舍候选**，不代入任何后端实现，也不新增权限码。仅供技术评审在接口定稿/增补时采纳或驳回；被驳回者 UI 一律回退“能力未开放”呈现，不阻塞 P0。

- **A. 空间列表容量字段**：`GET /api/drives`（修订 17.1）返回的每个 drive 是否含 `quota_bytes / used_bytes / reserved_bytes` 以及“我的权限 + isOwner/isManaging”标记？侧边个人容量条、左侧用量条、SpaceSwitcher 空间卡均依赖该段。若缺，需在返回对象中补 capacity + mine 段（契约补丁）。
- **B. “最近 / 收藏”导航根**：修订 IA §3.1 列出了最近/收藏入口，但其 per-user 表/接口（如 `tb_favorite`、`/api/recent`）未在修订中给出。候选：a) 后端新增 favorite/recent 数据与接口；b) MVP 隐藏两处导航，默认直入「我的文档」+ 搜索/结构浏览。UI 不实现“仅 local 假收藏”，该项技术定夺后开放页面。
- **C. 跨磁盘“移动/复制”**：修订 `POST /file/move`/`/file/copy` 未明确是否禁止跨 drive（跨盘在文件系统上非 O(1) rename，且无写入「复制下载上传」协议的语义说明）。UI 先保守：**移动/复制的目标选择默认限制在当前空间内**（面包屑/目录为同一 drive）；跨盘移动不在“拖放”上提供、改走“移动到…”对话框且仅在契约确认后才放跨盘选项，避免用户点错。
- **D. 网格缩略图与权限**：/t/{hash} 为只读预签名产物地址，业务已有预览权门控，但为避免“无预览权限的用户仍触发加载请求/侧信道”，UI 策略为：**仅当用户具备 FILE_PREVIEW 才预加载缩略**，否则回落类型图标；不需后端新增权限码。
- **E. 通知中心数据源**：修订无后端实时 push/poll 通知表/接口。UI 按“本地来源先行”：上传/删除/恢复/批量任务的完成与失败由前端 task store 驱动（通知铃收敛这些）；而“被加入空间/被提升权限/系统公告”等需后端通知能力，先以 disabled/缺失呈现，不强推（对应 §4.16）。
- **F. 恢复历史版本的精确权限码**：修订 17.3 的 restore 用 `FILE_RESTORE`；而“还原历史版本”实质上是产生一个新的版本对象（写入当前文件），可能还需文件写权对象持有。UI 一次性将该按钮权限映射为 `FILE_RESTORE`（必要时叠加 FILE_WRITE），最终判定行为由后端给（contract 待其确认）。
- **G. 审计详情明细获取形态**：审计列表中行的 `detail_json` 是否随行返回全文、还是要另按 id 拉取 → UI 的详情 Modal 以实际返回为准（不做二次虚构查询）。导出能力修订未给接口，MVP 面板只读展示（对应 §5.7）。
- **I. 管理概览统计接口**：修订 runtime 未含“上传量趋势(7d)/文件类型分布”等图表序列；管理概览先聚合仅呈现已有的统计字段，图类是否引入取决于后端是否补充统计接口，缺则省略以免伪造数据。
- **J. 数字格式约定**：站内文件大小统一采用 SI 十进制（kB/MB/GB，1000 进制），与修订字段语义一致；格式收敛于 UI `utils/format` 单源。
- **K. 删除=移入回收站统一**：桌面层“Delete”一律表示移入回收站、不做 shift+Del 直接销毁（修订 §12 purge 仅在回收站内可用）；如需桌面永久删除需后端新增接口并回到待确认重审。

> 上述项为「UI 契约边界」，不改变已拍板后端；只建议加入“接口增补清单”或等待其后端确认。UI 开发统一秉持：契约齐备→正常呈现；契约缺失→默认关闭/隐藏或保留 + tooltip 解释原因，绝不因后端暂缺而伪造可用的假按钮，也不阻塞 P0 推进。





